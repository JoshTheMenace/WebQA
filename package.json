{
  "name": "webqa",
  "version": "1.0.0",
  "description": "Autonomous browser testing with Playwright - execute tests via JSON DSL",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "postinstall": "npx playwright install chromium"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "playwright": "^1.49.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "^5.7.2"
  }
}
